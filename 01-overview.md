# 全体像

## 基本思想

AI 駆動開発は、AI にコードを書かせることだけではありません。重要なのは、AI が参照する情報、判断する順番、止まるべき場所、検証する方法を外部化することです。

この方式では、AI を「実装担当者」として扱うのではなく、次の制約を持つ開発エージェントとして扱います。

- 設計書、業務ルール、API 仕様、テスト契約を先に読む。
- 不明点はチャットで即質問せず、根拠付きの質問ファイルにする。
- 実装は最小範囲で行うが、仕様整合性を犠牲にしない。
- テスト、構造チェック、レビューを通過して初めて完了とする。
- 重要判断は作業ログ、設計書、PR ノートに残す。

## AI が drift する典型例

| drift | 内容 | 対策 |
|---|---|---|
| 終了条件の誤認 | 途中なのに「完了」と判断する | チェックリストと state ファイルで進捗を外部化する |
| 品質の自己過信 | テスト未実行や画面未確認でも良いと判断する | 検証コマンド、ブラウザ確認、レビュー観点を明文化する |
| 累積的なズレ | 小さな命名差、仕様差、責務差が積み重なる | Phase ごとに設計書と実装を照合する |
| 参照漏れ | 設計書や過去の判断を読まずに進める | skills に読み込み順を定義する |
| 既存実装への過適合 | 既存コードを正本扱いする | 仕様書、ADR、ルールを canonical source とする |

## 主要な成果物

| 成果物 | 役割 |
|---|---|
| `AGENTS.md` | AI エージェント向けの入口。作業順序、禁止事項、検証コマンドを書く |
| `CLAUDE.md` など | 別 AI ツール向けの同等入口。内容は `AGENTS.md` と揃える |
| `.claude/rules/` | 技術領域別の固定ルール。frontend、backend、infra、API など |
| `.agents/skills/` | Codex 向けの作業スキル。AIDLC、レビュー、テスト、PR 作成など |
| `deliverables/specs/` | 設計書、API 仕様、ドメイン仕様、アーキテクチャ正本 |
| `requirements/` | 要件定義、画面一覧、業務フローなど上流情報 |
| `businessRules/` | 業務ルール、受入条件、判断表 |
| `adr/` | Architecture Decision Record。例外や新規パターンの判断記録 |
| `tasks/lessons.md` | 過去の失敗と再発防止策 |
| `aidlc-docs/{date}_{task}/` | タスクごとの AIDLC 成果物、質問、監査ログ、状態管理 |

## 情報の優先順位

ルールが衝突した場合、必ず優先順位を決めます。例:

1. 要件、業務ルール、契約仕様
2. アーキテクチャ設計書、ADR
3. ドメイン設計書、API 仕様、DB 定義
4. コーディング規約、ディレクトリ規約
5. 既存コード
6. AI の推測

既存コードは重要な参考情報ですが、正本ではありません。既存コードと正本が矛盾する場合は、矛盾として記録し、実装方針を確認します。

## 人間との対話を減らす設計

AI は、設計書で解決できる質問を人間に投げません。人間に求めるのは、設計書間の矛盾、未定義の業務判断、アーキテクチャ例外、新規パターン採用など、実際に判断が必要な点だけです。

その判断もチャットで雑に聞くのではなく、次を含む質問ファイルにします。

- 背景と原理原則
- 用語定義
- 設計書上の根拠
- 外部リファレンス
- 選択肢の比較表
- AI の推奨と理由
- ユーザーの回答欄

## AI 駆動開発ではないもの

このガイドでいう AI 駆動開発は、次のような使い方だけを意味しません。

- チャットへ要望を書き、生成されたコードをそのまま採用する
- 人間が決めていない仕様を AI に推測させる
- test を書かず、AI の「できました」を完了判定にする
- レビュー、権限、CI をすべて AI の注意力へ置き換える
- 大量のルールを一度にpromptへ貼り付ける

AI は高速に探索・生成できますが、仕様の所有者や最終的な責任主体にはなりません。人間は目的、業務判断、許容リスク、承認を担当し、AI は調査、比較、実装、検証、記録を支援します。

## 具体例: 「予約キャンセル機能を追加して」

文書とskillがない場合、AIは既存コードから次を推測する可能性があります。

- 誰がキャンセルできるか
- どの状態からキャンセルできるか
- キャンセル料が発生するか
- APIのstatusとerror形式
- 通知や在庫復元が必要か

推測のまま実装すると、動くコードでも業務的には誤っている可能性があります。このガイドの方式では、次のように進めます。

```text
依頼
  ↓
入口ガイドでbackend作業に分類
  ↓
予約business rule、domain spec、API spec、関連ADRを読む
  ↓
未定義事項だけを質問する
  ↓
キャンセル可能条件をtest契約にする
  ↓
domain → application → infrastructure → presentationの順で実装
  ↓
unit / integration / architecture testを実行
  ↓
仕様、実装、testの差分を報告する
```

「コードを書く前後に何をするか」まで外部化している点が、単なるコード生成との違いです。

## 何が改善されるのか

| 課題 | 外部化するもの | 期待する改善 |
|---|---|---|
| セッションごとに手順が違う | skill | 作業順序の再現性 |
| 仕様を推測する | spec、business rule | 業務driftの減少 |
| 判断理由が消える | ADR、decision log | 再議論と誤った巻き戻しの減少 |
| 完了判定が主観的 | test契約、quality gate | 未検証完了の減少 |
| 危険操作を注意書きだけで防ぐ | hook、permission、CI | 機械的な事故防止 |
| AIごとに違う手順を使う | 共通masterとadapter | ツール間driftの減少 |

これらは導入しただけで自動的に改善するものではありません。trigger精度、検証結果、手戻り、障害流出などを計測し、skillと文書を更新する必要があります。

## 用語の最小整理

| 用語 | このガイドでの意味 |
|---|---|
| context | AIが現在の判断に利用できる依頼、会話、文書、tool結果 |
| drift | 仕様、設計、実装、test、文書の間に生じるずれ |
| SSoT / 正本 | ある情報について最終的に参照する所有元 |
| rule | 対象領域で守る制約 |
| skill | 特定作業の再利用可能な手順 |
| ADR | 重要な設計判断と理由の記録 |
| quality gate | 次工程へ進む前に満たす機械的・人的条件 |
| progressive disclosure | 必要な情報だけを段階的に読み込む設計 |
