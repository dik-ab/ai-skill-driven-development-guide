# サンプルプロジェクト エージェント入口

> **これは勉強会用のライブデモです。** このリポジトリで Claude Code を起動すると、
> このファイルが毎セッション自動で読み込まれます。架空のアプリ
> （`apps/frontend` + `apps/backend` を持つモノレポ）を想定して書いています。
> 解説は `12-live-demo-walkthrough.md`。

## 作業順序

1. 変更対象を frontend / backend / docs に分ける。
2. 対象に合う `.claude/rules/*.md` を読む。
3. 設計判断は設計書（正本）と照合してから実装する。
4. 変更は最小にし、対象範囲の検証を実行する。
5. 未解決スコープ・失敗した検証・設計書との矛盾は隠さず報告する。

## 必須ルール（全タスク共通のものだけをここに置く）

- コード・識別子・コミットは英語。UI 文言とユーザー向けドキュメントは日本語。
- 数値目標は `rg` / `wc` 等のコマンドで実測して検証する。推測で書かない。
- 仕様通りに実装できない場合、黙って代替実装しない。停止して判断を仰ぐ。
- レビュー結果を「問題なさそう」で終えない。指摘ゼロなら「ゼロ」と明言する。

## 参照先

- 領域別ルール: `.claude/rules/`（frontend.md / backend.md）
- タスク種別の手順書: `.claude/skills/`（feature-dev / pr-review）
- 設計書の正本: `../sample-specs/`（このデモでは架空。実プロジェクトでは設計書リポジトリを指す）
