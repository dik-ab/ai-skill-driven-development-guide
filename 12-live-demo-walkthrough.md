# 12. 勉強会60分ガイド — 1つの型で全部説明する

このリポジトリのルートには実際に動く `.claude/` 一式が置いてある。
60分の発表は、次の**一文**を5つの領域で繰り返し見せるだけで成立する。

> **AIへの指示は「常駐する入口 → 領域のrules → タスクのskills → 正本」の4層に分ける。
> 応用領域（レビュー・AWS・Slack・図面・ブラウザ試験）が増えても、増えるのはskill 1枚。**

聞き手が持ち帰るのはこの型だけでいい。以降の各節は「見せるもの／言うこと／なぜそうなるか」の
3点セットで、そのまま台本として使える。

## タイムテーブル

| 時間 | 節 | 内容 |
|---|---|---|
| 0–5 | 12.1 | 型の宣言。今日の全デモはこの1枚の図の上にある |
| 5–20 | 12.2 | 基礎: 3層の読み込みと「このPRをレビューして」のトレース |
| 20–30 | 12.3 | AWS連携: skill + IAM で自律範囲を物理的に区切る |
| 30–35 | 12.4 | Claude in Slack: 入口が変わるだけで、同じ型が効く |
| 35–45 | 12.5 | draw.io: 図面もテキストだから同じ型に乗る |
| 45–55 | 12.6 | ブラウザ試験自動化: 「完了」の定義をskillに書く |
| 55–60 | 12.7 | 締め: 明日からやる1つ |

---

## 12.1 型の宣言（5分）

**見せるもの**: このリポジトリのツリー1枚。

```
CLAUDE.md                     ← 入口。毎セッション必ず読まれる。だから薄い
.claude/rules/                ← 領域の不変条件。対象パスを触るときだけ読まれる
    frontend.md  backend.md
.claude/skills/               ← タスク種別の手順書。呼ばれたときだけ本文が読まれる
    feature-dev/  pr-review/  aws-readonly/  drawio-diagram/  browser-test/
（設計書=正本は別リポジトリ。skillsとrulesがポインタで指す）
```

**言うこと**: 「AIに読ませるファイルは3種類しかない。全部を最初に読むわけではなく、
読み込みタイミングが違う。この違いが設計の本体。skill一覧を見てほしい——
レビューもAWSもdraw.ioもブラウザ試験も、**全部ただのskill**。今日はこれを順に開けていく」

---

## 12.2 基礎: 読み込み順とレビューのトレース（15分）

### 読み込みは3層

| 層 | 何が | いつ | 教訓 |
|---|---|---|---|
| 常駐 | `CLAUDE.md` 全文 + 各skillの**説明文だけ** | セッション開始時、毎回 | 一等地。家賃を毎回払うので薄く保つ |
| 条件付き | `rules/*.md` | 対象パスを触るとき | 領域外のルールを混ぜない |
| オンデマンド | SKILL.md **本文**・設計書 | skillが起動されたとき | 重い手順はここへ逃がす |

**言うこと**: 「全ルールを入口に書くと、タイポ修正のセッションでもレビュー観点カタログを
読ませることになり、本当に守ってほしいルールが埋もれる。skillは frontmatter の説明文だけが
常駐する『目次だけ常駐、本文は貸出式』の図書館方式」

### トレース: 「このPRをレビューして」

ユーザーの入力は一言。そこから起こることを順に実況する（ここはライブデモ推奨）。

```
「このPRをレビューして」
 ① skill一覧の説明文とマッチ → pr-review 起動（feature-devは "NOT for: レビュー" で除外）
 ② SKILL.md 本文がここで初めて読まれる
 ③ 手順に従い diff 取得 → 変更が backend なら rules/backend.md をここで読む
 ④ 観点別サブエージェント起動（SPEC / REGRESSION / TEST / SECURITY）
    ・1エージェント1観点。渡すのは diff と仕様だけ（作者バイアス排除）
    ・「壊れるシナリオを3つ挙げよ」の命令形。「問題ないか確認」はLGTMに寄る
 ⑤ 指摘を裏取り → 深刻度順に報告。指摘ゼロの観点は「ゼロ」と明言
```

**言うこと**: 「観点分割も作者バイアス排除も裏取りも、俺が毎回指示したんじゃない。
**skillに一度書いたから毎回起こる**。人間の修正が永続ルールになる、の最小実装がこれ」

### どこに何を置くか（1枚で）

- **全タスクで守るもの** → `CLAUDE.md`（例: 仕様通りに作れないなら停止して聞け）
- **領域の不変条件** → `rules/`（例: domain層はframeworkに依存しない）
- **タスクの進め方** → `skills/`（例: レビューは観点別に分割せよ）
- **仕様・契約・判断の記録** → 設計書（人間も読む一次情報）

原則は2つ: 「**何を守るかはrules、どう進めるかはskills**」「**下の層に置けるものを上に置かない**」。

---

## 12.3 AWS連携: 自律範囲は IAM で区切る（10分）

**見せるもの**: `.claude/skills/aws-readonly/SKILL.md`（20行しかない）。

**言うこと**: 「『ログ見て』と言うだけでAIがCloudWatchを調べてくれる。仕組みは12.2と
完全に同じで、説明文の『ログ見て』がトリガーになってskillが起動するだけ。
ここで大事なのはskillの中身の1行——」

```bash
AWS_PROFILE=ai-readonly aws logs tail /app/backend --since 30m
```

「`ai-readonly` プロファイルにはReadOnlyAccessしか付いていない。つまりAIが本番を壊さないことは
**プロンプトのお願いではなくIAMが保証**する。ガードレールはテスト・レビュー・権限の3点セットで、
権限だけが唯一『物理的』に効く。skillに書く手順は『守れ』ではなく
『この範囲なら自由に動いていい』という**自律のライセンスの宣言**になる」

補足（質問が出たら）: ReadOnlyAccessはS3のGetObjectなどデータ読み取りも含む。メタデータだけ
見せたいならViewOnlyAccessの方が狭い。長期アクセスキーよりAssumeRoleの短期クレデンシャルが安全。

---

## 12.4 Claude in Slack: 入口が変わるだけ（5分）

**見せるもの**: スライド1枚（またはSlack画面）。新しいファイルは**1枚もない**。

**言うこと**: 「Slackから同じリポジトリにタスクを依頼すると、リポジトリ側の
`CLAUDE.md`・rules・skillsが**そのまま効く**。チャットUIが変わっただけで、
読み込みの型は12.2と同一。つまり——

- 障害っぽいときにSlackで『ログ見て』→ 12.3のaws-readonly skillが動く
- レビュー依頼をSlackから投げる → 12.2のpr-review skillが動く

**投資先をチャットの使い方ではなくリポジトリのファイルにしたから、入口が増えても資産が流用できる**。
これがskill駆動にする一番の実利」

（注意: Slack連携のセットアップ手順・機能範囲は製品バージョンで変わる。デモ前に手元で確認）

---

## 12.5 draw.io: 図面もテキストだから同じ型に乗る（10分）

**見せるもの**: `.claude/skills/drawio-diagram/SKILL.md` と、実際に生成した `.drawio` → PNG。

**言うこと**: 「draw.ioのファイル実体はXMLのテキスト。だから——

1. AIが直接生成・編集できる（『構成図描いて』でskill起動）
2. CLIでヘッドレスにPNG化できる: `drawio -x -f png -o out.png diagram.drawio`
3. XMLをコミットすれば**図面がdiffレビューとバージョン管理の対象**になる

つまり図面が『ドキュメント腐敗の例外』ではなくなる。設計変更に図が追従していなければ、
コードレビューと同じく指摘できる。置き場所は設計書リポジトリ（正本）の隣で、
skillはそこへのポインタを持つ——ドキュメントの置き方の話（12.2の4層）とここで繋がる」

---

## 12.6 ブラウザ試験自動化: 「完了」の定義をskillに書く（10分）

**見せるもの**: `.claude/skills/browser-test/SKILL.md` と、Playwrightの実演（**録画バックアップ必須**。
ライブ事故率が最も高いデモ）。

**言うこと**: 「『画面で動作確認して』でAIがブラウザを操作し、スクショ付きで報告してくる。
このskillの本当の価値は自動操作そのものじゃなくて、**冒頭の前提2行**——

> 『テストが通った』と『画面で動く』は別。リファクタ後はユニットテストgreenでも画面確認までやって完了とする。

昔、確認手順を書いていなかったせいで、AIが動作確認不十分のまま『完了』と報告してきた失敗があった。
その教訓を『気をつけよう』ではなく**skillの手順**にした。以来、同じ失敗は起きていない。
事故→実行可能なチェック化、という学習ループの実物がこれ」

---

## 12.7 締め（5分）

**言うこと**: 「今日見せたのは5つの応用に見えて、型は1つだった。
レビューもAWSもSlackもdraw.ioもブラウザ試験も、**skill 1枚を足しただけ**。

明日からやるなら: 自分のリポジトリに `CLAUDE.md` 1枚と skill 1枚（おすすめはPRレビュー）を
置くこと。今日のリポジトリは公開してあるので、そのまま雛形にしてほしい。
AIに任せられる範囲は、こうやって検証とレビューを設計した範囲に比例して広がる。
だからエンジニアの仕事はなくならず、この設計——上流に移動する」

---

## 付録: デモ前チェックリスト

- [ ] このリポジトリで `claude` を起動し、skill一覧が5枚見えることを確認
- [ ] 適当なdiffを用意し「レビューして」が pr-review に解決されることを確認
- [ ] `ai-readonly` プロファイルが手元にあるか（無ければ12.3はスライドのみ）
- [ ] `drawio` CLIの動作確認（`brew install --cask drawio`）
- [ ] ブラウザ試験デモの**録画**を用意（ライブが死んだら即切り替え）
- [ ] skillの自動読込・frontmatter仕様はバージョンで変わる。当日と同じ環境で通しておく
