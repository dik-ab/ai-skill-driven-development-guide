# 12. ライブデモ解説 — Claude はファイルをどの順で読むか

この章は勉強会用。このリポジトリのルートには**実際に動く最小構成の `.claude/` 一式**が
置いてあり、ここで Claude Code を起動するとそのままデモになる。
本章はその構成の解説と、「このPRをレビューして」と言ったときに何が起こるかのトレース。

## 12.1 デモの構成（ファイル 6 つ）

```
ai-skill-driven-development-guide/
├── CLAUDE.md                          ← 入口。毎セッション自動読込
└── .claude/
    ├── rules/
    │   ├── frontend.md                ← 領域ルール。paths スコープ付き
    │   └── backend.md                 ← 領域ルール。paths スコープ付き
    └── skills/
        ├── feature-dev/SKILL.md       ← 実装タスクの手順書
        └── pr-review/SKILL.md         ← レビュータスクの手順書
```

実プロジェクトではこれが rules 10 本・skills 27 本に育つが、**構造は同じ**。
最小構成で仕組みを理解すれば、あとは同じ型を増やすだけ。

## 12.2 読み込みは 3 層 — 全部が最初に読まれるわけではない

一番誤解されやすいのがここ。Claude はこれらのファイルを「全部最初に読む」のではない。
読み込みタイミングが 3 層に分かれていて、この分離こそが設計の本体。

| 層 | 何が | いつ読まれるか | コンテキスト消費 |
|---|---|---|---|
| **常駐** | `CLAUDE.md` 全文 + 各 skill の **frontmatter（name/description）だけ** | セッション開始時、毎回 | 常に払う。だから薄く保つ |
| **条件付き** | `.claude/rules/*.md` | `paths` にマッチするファイルを触るとき（スコープ無しなら常時） | 触った領域の分だけ |
| **オンデマンド** | SKILL.md の**本文**、設計書の正本 | skill が起動されたとき / 手順がポインタで指したとき | 必要なタスクでだけ |

なぜこう分けるか。コンテキストウィンドウは有限で、常駐させたものは
**すべてのタスクで毎回コストを払う**。全ルールを CLAUDE.md に書くと、
タイポ修正のセッションでもレビュー観点カタログを読み込むことになり、
本当に守ってほしいルールが埋もれる（長い指示ほど個々の遵守率は下がる）。

そこで常駐層には「判断の順序」と「全タスク共通の絶対ルール」だけを置き、
詳細は**ポインタ**にして、必要になった瞬間に読ませる。
skill の frontmatter は言わば「目次だけ常駐、本文は貸出式」の図書館方式。

## 12.3 トレース — 「このPRをレビューして」の一部始終

デモで実際に見せる流れ。ユーザーが一言タイプしてから報告が返るまで。

```
ユーザー: 「このPRをレビューして」
```

**Step 0 — この時点で Claude が持っている情報（常駐層）**
- CLAUDE.md 全文（作業順序・必須ルール・参照先）
- skill 一覧: `pr-review: PR・ブランチの変更をレビューする。Triggers on "レビューして"...`
  `feature-dev: 機能の追加・変更を実装する。Triggers on "実装して"...`
- rules の本文はまだ**読んでいない**。SKILL.md の本文もまだ読んでいない。

**Step 1 — skill の選択**
依頼文「レビューして」が skill 一覧の description とマッチ。
`feature-dev` は "NOT for: レビュー" と明記されているので除外され、`pr-review` が起動される。
→ ここで初めて `pr-review/SKILL.md` の**本文**がコンテキストに展開される。

description がトリガー条件を兼ねるので、**description の書き方が skill 選択の精度を決める**。
「いつ使うか」だけでなく「いつ使わないか（NOT for）」を書くのは、
隣接する skill との取り違えを防ぐため。

**Step 2 — 対象の確定と rules の読み込み**
SKILL.md の手順に従い `git diff origin/main`（または `gh pr diff`）で変更ファイルを取得。
変更が `apps/backend/**` に触れていれば、ここで初めて `.claude/rules/backend.md` を読む。
→ **レビュー基準は skill が持たない。** skill が持つのは手順で、基準は rules と設計書にある。
だからレビュー基準を変えたいとき、skill を触らず rules を直せばよい（関心の分離）。

**Step 3 — 観点別サブエージェントの起動**
SKILL.md の指示で、SPEC / REGRESSION / TEST /（該当すれば SECURITY）の
観点ごとに独立したサブエージェントを起動。各エージェントに渡すのは **diff と仕様だけ**。
実装時の経緯や意図の説明は渡さない——意図を読むと納得してしまう（作者バイアス）。
ミッションは「問題ないか確認して」ではなく「壊れるシナリオを 3 つ挙げよ」の命令形。

**Step 4 — 裏取りと報告**
挙がった指摘を裏取りして false positive を落とし、深刻度順に報告。
「指摘ゼロの観点はゼロと明言」「見ていない観点も明記」までが手順。

```
報告: critical 1 件（○○で NPE）、minor 2 件。
      SPEC 観点は指摘ゼロ。SECURITY 観点は対象外（入力処理の変更なし）。
```

ポイント: ユーザーの一言は「レビューして」だけ。**観点分割も作者バイアス排除も
裏取りも、人間が毎回指示したのではなく、skill に一度書いたから毎回起こる。**
これが「人間の修正が永続的なルールになる」（AI-DLC の学習ループ）の最小実装。

## 12.4 どこに何のルールを置くべきか — 判断基準

新しいルールを書きたくなったとき、置き場所はこの表で決める。

| 置き場所 | 置くもの | 判断基準 | 例 |
|---|---|---|---|
| `CLAUDE.md` | 判断の順序・全タスク共通の絶対ルール | **すべての**タスクで守るべきか？ | 「仕様通りに作れないなら停止して聞け」 |
| `.claude/rules/` | 領域固有の不変条件 | 特定の**領域**を触るとき常に守るべきか？ | 「domain 層は framework に依存しない」 |
| `.claude/skills/` | タスク種別の**手順** | 特定の**種類のタスク**の進め方か？ | 「レビューは観点別に分割せよ」 |
| 設計書（正本） | 仕様・契約・判断の記録 | 人間も読む一次情報か？ | ドメインモデル、API 契約、ADR |

迷ったときの原則が 2 つ:

1. **「何を守るか」は rules / 設計書、「どう進めるか」は skills。**
   混ぜると、手順を変えるたびに基準まで書き換わる（逆も然り）。
2. **下の層に置けるものを上の層に置かない。**
   常駐層（CLAUDE.md）は一等地で、置いたものすべてに毎セッション家賃がかかる。
   「これは全タスクで必要か？」に No なら rules へ、
   「これは領域を触るとき常に必要か？」にも No なら skills へ降ろす。

もうひとつ、実運用で効くのが**事故のルール化の置き場所**。
「前にこういう見落としがあった」という教訓は、思い出話ではなく
該当 skill の手順に**実行可能なチェック**（grep せよ、○○を確認せよ）として追記し、
直下に事故の経緯を「背景」として残す。ルールだけだと後から読む AI（と人間）が
意図を誤解し、経緯だけだと手順にならない。両方書く。

## 12.5 勉強会でのデモ手順

1. このリポジトリで `claude` を起動し、`/context` や冒頭の応答で
   CLAUDE.md が読み込まれていることを見せる。
2. 「利用可能な skill を一覧して」と聞き、frontmatter だけが見えている状態を見せる。
3. 適当な diff を作って「レビューして」と言い、
   pr-review 起動 → rules 読み込み → 観点別サブエージェント、の流れを実況する。
4. 最後に 12.4 の表で「今日持ち帰るのはこの型」と締める。

> 注意: skill の自動読込場所・frontmatter の仕様（`paths` スコープ含む）は
> クライアントのバージョンで変わる。デモ前に手元のバージョンで一度通しておくこと。
